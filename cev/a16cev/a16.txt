fonçoes (são ações executadas assim que são chamadas ou em decorrência de algum evento)
agora sobre passagem de parametros, retorno de resultados.

#chamada //nem toda funçao usar parametros e nem tem retorno
parametros
acao
retorno

function ação(param){ para. formal


    return res (retorno)
}
ação(5) parametro real

function parimp(n){
    if (n% 2==0){
        return'par'
    }else{
        return 'impar'
    }
}
let res = parimp(11)